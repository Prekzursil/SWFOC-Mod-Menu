# rva_player_list_switch_sides

_Auto-generated by `tools/generate_feature_pages.py` from `verified_facts.json` + `callgraph_index.sqlite`._

## Ledger entry

- **fact_id:** `rva_player_list_switch_sides`
- **rva:** `0x297E80` (abs `0x140297E80`)
- **category:** `engine_function`
- **confidence:** `VERIFIED`
- **first_documented:** 2026-04-10
- **last_verified:** 2026-04-26
- **resolved name:** `sub_140297E80`
- **size:** 571 bytes
- **on-disk decompiles:** IDA=YES, Ghidra=YES, Binja=YES

## Claim

PlayerListClass::Switch_Sides at RVA 0x297E80. Canonical method for rotating the human-controlled player slot. Takes a PlayerListClass* (the global at 0xA16FD0). Increments *(int*)(this + 0x30), wraps at vector end, skips players whose inner +0x10A flag is 0, sets the new slot's PlayerObject+0x62 byte to 1, iterates the entire PlayerArray and clears every other player's +0x62 byte to 0, logs 'PlayerListClass::Switch_Sides: Switched to player index %d, faction %s', then fires event 55 through the active game mode's event dispatcher at *(GameMode + 24) to notify subsystems (camera / HUD / input router). This is the SETTER path the previous session's B4 investigation could not find on disk; the CE trainer does not use it (trainer has no switch-player button), which is why read-only grep missed it.

## Notes

Resolves the 2026-04-09 BLOCKED-NEEDS-LIVE-INVESTIGATION state for FactionSwitchService. The previous session's investigation doc searched for write paths to PlayerObject+0x62 directly and found none — that was the wrong question. The canonical API is not a single memory write; it is a class method that encapsulates the entire side-effect chain (slot flip, array sweep, event dispatch, subsystem refresh). Bridge wiring deferred to next session per handoff brief Section 6 priority ordering.

## Tool findings

### ida_pro


### cheat_engine


### ghidra

- **evidence:** `Ghidra MCP decompile_function_by_address bulk verification 2026-04-26 — same binary loaded; functions resolve at the claimed RVAs (image base 0x140000000).`
- **verified_at:** 2026-04-26T00:00:00Z

### binary_ninja

- **evidence:** `Binary Ninja MCP function_at 2026-04-26 — function present at this RVA (image_base + RVA = 0x140 prefix) in StarWarsG.exe view 4. Bulk-verified across the engine_function + lua_binding cohort.`
- **verified_at:** 2026-04-26T00:00:00Z

## Direct callers (3 shown)

| addr | name |
|---|---|
| `0x14001FB30` | `sub_14001FB30` |
| `0x1400C0110` | `sub_1400C0110` |
| `0x140456970` | `sub_140456970` |

## Direct callees (5 shown)

| addr | name |
|---|---|
| `0x14001FA90` | `_guard_check_icall_nop` |
| `0x140025760` | `sub_140025760` |
| `0x1402814D0` | `sub_1402814D0` |
| `0x14028AF60` | `sub_14028AF60` |
| `0x1402A9FF0` | `sub_1402A9FF0` |

---

Source: [`verified_facts.json`](../verified_facts.json) entry `rva_player_list_switch_sides`. 
Re-generate via `python tools/generate_feature_pages.py`.
