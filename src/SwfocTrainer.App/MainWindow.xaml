<Window x:Class="SwfocTrainer.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SWFOC Trainer Editor" Height="900" Width="1400"
        MinWidth="1024" MinHeight="700"
        Background="#111822"
        Foreground="#E6EDF7">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Profile:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Width="320" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfileId}"/>
            <Button Content="Load Profiles" Command="{Binding LoadProfilesCommand}" Margin="8,0,0,0"/>
            <Button Content="Load Actions" Command="{Binding LoadActionsCommand}" Margin="8,0,0,0"/>
            <Button Content="Launch + Attach" Command="{Binding LaunchAndAttachCommand}" Margin="8,0,0,0"/>
            <Button Content="Attach" Command="{Binding AttachCommand}" Margin="8,0,0,0"/>
            <Button Content="Detach" Command="{Binding DetachCommand}" Margin="8,0,0,0"/>
            <TextBlock Text="  Launch:" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBox Width="80" Text="{Binding LaunchTarget, UpdateSourceTrigger=PropertyChanged}" ToolTip="Swfoc or Sweaw"/>
            <TextBox Width="90" Margin="6,0,0,0" Text="{Binding LaunchMode, UpdateSourceTrigger=PropertyChanged}" ToolTip="Vanilla, SteamMod, or ModPath"/>
            <TextBox Width="150" Margin="6,0,0,0" ToolTip="Workshop IDs (comma-separated, parent first)" Text="{Binding LaunchWorkshopId, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="170" Margin="6,0,0,0" ToolTip="MODPATH" Text="{Binding LaunchModPath, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Clean Start" Margin="6,0,0,0" IsChecked="{Binding TerminateExistingBeforeLaunch}" VerticalAlignment="Center"/>
            <TextBlock Text="  Mode:" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBlock Text="{Binding RuntimeMode}" VerticalAlignment="Center"/>
            <TextBlock Text="  Override:" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <ComboBox Width="120"
                      ItemsSource="{Binding RuntimeModeOverrideOptions}"
                      SelectedItem="{Binding RuntimeModeOverride, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="  Effective:" VerticalAlignment="Center" Margin="8,0,4,0"/>
            <TextBlock Text="{Binding EffectiveRuntimeMode}" VerticalAlignment="Center"/>
            <TextBlock Text="  Symbols:" VerticalAlignment="Center" Margin="12,0,4,0"/>
            <TextBlock Text="{Binding ResolvedSymbolsCount}" VerticalAlignment="Center"/>
        </StackPanel>

        <TabControl Grid.Row="1" Background="#182132" BorderThickness="0">
            <TabItem Header="Runtime">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Action:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="380" ItemsSource="{Binding Actions}" SelectedItem="{Binding SelectedActionId}"/>
                        <Button Content="Execute" Command="{Binding ExecuteActionCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <!-- Quick-Action Buttons -->
                    <GroupBox Header="Quick Actions" Grid.Row="1" Margin="0,10,0,0" Padding="6">
                        <StackPanel>
                            <!-- Credits row: value input + freeze toggle + set button -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <TextBlock Text="💰 Credits:" VerticalAlignment="Center" Margin="4,0,6,0"/>
                                <TextBox Text="{Binding CreditsValue, UpdateSourceTrigger=PropertyChanged}" Width="110" VerticalAlignment="Center" Padding="4,2"/>
                                <CheckBox Content="Freeze" IsChecked="{Binding CreditsFreeze}" VerticalAlignment="Center" Margin="8,0,8,0"/>
                                <Button Content="Set Credits" Command="{Binding QuickSetCreditsCommand}" Padding="8,4"/>
                            </StackPanel>
                            <WrapPanel>
                                <Button Content="⏱️ Freeze Timer" Command="{Binding QuickFreezeTimerCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="🗺️ Toggle Fog" Command="{Binding QuickToggleFogCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="🤖 Toggle AI" Command="{Binding QuickToggleAiCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="⚡ Instant Build Patch" Command="{Binding QuickInstantBuildCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="🏗️ Unit Cap 99k" Command="{Binding QuickUnitCapCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="🛡️ God Mode" Command="{Binding QuickGodModeCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="💥 One Hit Kill" Command="{Binding QuickOneHitCommand}" Margin="2" Padding="8,4"/>
                                <Button Content="🔓 Unfreeze All" Command="{Binding QuickUnfreezeAllCommand}" Margin="2" Padding="8,4"/>
                            </WrapPanel>
                        </StackPanel>
                    </GroupBox>

                    <Grid Grid.Row="2" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Payload JSON" Margin="0,0,8,0">
                            <TextBox Text="{Binding PayloadJson}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                        </GroupBox>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Active Freezes Panel -->
                            <GroupBox Header="Active Freezes" Margin="0,0,0,8">
                                <ListBox ItemsSource="{Binding ActiveFreezes}" Height="80" FontFamily="Consolas" FontSize="11"/>
                            </GroupBox>

                            <GroupBox Header="Catalog, Helper &amp; Hotkeys" Grid.Row="1">
                                <Grid Margin="8">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Load Catalog" Command="{Binding LoadCatalogCommand}"/>
                                        <Button Content="Deploy Helper" Command="{Binding DeployHelperCommand}" Margin="8,0,0,0"/>
                                        <Button Content="Verify Helper" Command="{Binding VerifyHelperCommand}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <ListBox Grid.Row="1" ItemsSource="{Binding CatalogSummary}" Margin="0,8,0,0" Height="100"/>
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Content="Load Hotkeys" Command="{Binding LoadHotkeysCommand}"/>
                                        <Button Content="Save Hotkeys" Command="{Binding SaveHotkeysCommand}" Margin="8,0,0,0"/>
                                        <Button Content="Add" Command="{Binding AddHotkeyCommand}" Margin="8,0,0,0"/>
                                        <Button Content="Remove" Command="{Binding RemoveHotkeyCommand}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <DataGrid Grid.Row="3"
                                              ItemsSource="{Binding Hotkeys}"
                                              SelectedItem="{Binding SelectedHotkey}"
                                              AutoGenerateColumns="False"
                                              CanUserAddRows="False"
                                              Margin="0,8,0,0">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Gesture" Binding="{Binding Gesture}" Width="*"/>
                                            <DataGridTextColumn Header="Action" Binding="{Binding ActionId}" Width="*"/>
                                            <DataGridTextColumn Header="Payload JSON" Binding="{Binding PayloadJson}" Width="2*"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </GroupBox>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Live Ops">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Refresh Reliability" Command="{Binding RefreshActionReliabilityCommand}"/>
                        <Button Content="Load Spawn Presets" Command="{Binding LoadSpawnPresetsCommand}" Margin="8,0,0,0"/>
                        <Button Content="Capture Unit Baseline" Command="{Binding CaptureSelectedUnitBaselineCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <GroupBox Header="Selected Unit Transaction Lab" Margin="0,0,0,10">
                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Hull HP" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Column="1" Text="{Binding SelectedUnitHp}" Margin="0,0,8,6"/>
                                <TextBlock Grid.Column="2" Text="Shield" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Column="3" Text="{Binding SelectedUnitShield}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="1" Text="Speed" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedUnitSpeed}" Margin="0,0,8,6"/>
                                <TextBlock Grid.Row="1" Grid.Column="2" Text="Damage x" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding SelectedUnitDamageMultiplier}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="2" Text="Cooldown x" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedUnitCooldownMultiplier}" Margin="0,0,8,6"/>
                                <TextBlock Grid.Row="2" Grid.Column="2" Text="Veterancy" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding SelectedUnitVeterancy}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="3" Text="Owner Faction" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedUnitOwnerFaction}" Margin="0,0,8,6"/>

                                <StackPanel Grid.Row="4" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Apply Draft" Command="{Binding ApplySelectedUnitDraftCommand}"/>
                                    <Button Content="Revert Last" Command="{Binding RevertSelectedUnitTransactionCommand}" Margin="8,0,0,0"/>
                                    <Button Content="Restore Baseline" Command="{Binding RestoreSelectedUnitBaselineCommand}" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <GroupBox Grid.Row="1" Header="Spawn Preset Studio">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Preset" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <ComboBox Grid.Column="1" Grid.ColumnSpan="3"
                                          ItemsSource="{Binding SpawnPresets}"
                                          SelectedItem="{Binding SelectedSpawnPreset}"
                                          DisplayMemberPath="Name"
                                          Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="1" Text="Quantity" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SpawnQuantity}" Margin="0,0,8,6"/>
                                <TextBlock Grid.Row="1" Grid.Column="2" Text="Delay (ms)" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding SpawnDelayMs}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="2" Text="Faction" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedFaction}" Margin="0,0,8,6"/>
                                <TextBlock Grid.Row="2" Grid.Column="2" Text="Entry Marker" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding SelectedEntryMarker}" Margin="0,0,0,6"/>

                                <StackPanel Grid.Row="3" Grid.ColumnSpan="4" Orientation="Horizontal">
                                    <CheckBox Content="Stop on first failure" IsChecked="{Binding SpawnStopOnFailure}" VerticalAlignment="Center"/>
                                    <Button Content="Run Spawn Batch" Command="{Binding RunSpawnBatchCommand}" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                    </Grid>

                    <Grid Grid.Column="1" Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <GroupBox Header="Operational Diagnostics">
                            <ListBox ItemsSource="{Binding LiveOpsDiagnostics}" Height="110"/>
                        </GroupBox>

                        <GroupBox Grid.Row="1" Header="Calibrate Symbol" Margin="0,10,0,0">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="180"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Symbol" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Column="1" Text="{Binding CalibrationTargetSymbol}" Margin="0,0,8,6"/>
                                <Button Grid.Column="2"
                                        Content="Scan"
                                        Command="{Binding ScanCalibrationCandidatesCommand}"
                                        Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="1"
                                           Grid.ColumnSpan="3"
                                           Text="{Binding CalibrationScanSummary}"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,8"/>

                                <DataGrid Grid.Row="2"
                                          Grid.ColumnSpan="3"
                                          ItemsSource="{Binding CalibrationCandidates}"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Pattern" Binding="{Binding SuggestedPattern}" Width="2*"/>
                                        <DataGridTextColumn Header="Offset" Binding="{Binding Offset}" Width="*"/>
                                        <DataGridTextColumn Header="Address Mode" Binding="{Binding AddressMode}" Width="*"/>
                                        <DataGridTextColumn Header="Type" Binding="{Binding ValueType}" Width="*"/>
                                        <DataGridTextColumn Header="RVA" Binding="{Binding InstructionRva}" Width="*"/>
                                        <DataGridTextColumn Header="Hits" Binding="{Binding ReferenceCount}" Width="*"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </GroupBox>

                        <GroupBox Grid.Row="2" Header="Action Reliability" Margin="0,10,0,0">
                            <DataGrid ItemsSource="{Binding ActionReliability}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Action" Binding="{Binding ActionId}" Width="2*"/>
                                    <DataGridTextColumn Header="State" Binding="{Binding State}" Width="*"/>
                                    <DataGridTextColumn Header="Reason" Binding="{Binding ReasonCode}" Width="2*"/>
                                    <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>

                        <GroupBox Grid.Row="3" Header="Selected Unit Transaction History" Margin="0,10,0,0">
                            <DataGrid ItemsSource="{Binding SelectedUnitTransactions}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Txn" Binding="{Binding TransactionId}" Width="*"/>
                                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="*"/>
                                    <DataGridTextColumn Header="Rollback" Binding="{Binding IsRollback}" Width="*"/>
                                    <DataGridTextColumn Header="Operation" Binding="{Binding Operation}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Save Editor">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Save:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Text="{Binding SavePath}" Width="760"/>
                        <Button Content="Browse" Command="{Binding BrowseSaveCommand}" Margin="8,0,0,0"/>
                        <Button Content="Load" Command="{Binding LoadSaveCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,0">
                        <TextBlock Text="Node Path:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Text="{Binding SaveNodePath}" Width="360"/>
                        <TextBlock Text="Value:" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Text="{Binding SaveEditValue}" Width="180"/>
                        <Button Content="Edit" Command="{Binding EditSaveCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10,0,0">
                        <Button Content="Validate" Command="{Binding ValidateSaveCommand}"/>
                        <Button Content="Preview Diff" Command="{Binding RefreshDiffCommand}" Margin="8,0,0,0"/>
                        <Button Content="Write Edited Save" Command="{Binding WriteSaveCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <GroupBox Grid.Row="3" Header="Save Lab Patch Packs" Margin="0,14,0,0">
                        <Grid Margin="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="180"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
                                <TextBlock Text="Patch Pack:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Text="{Binding SavePatchPackPath}" Width="540"/>
                                <Button Content="Browse" Command="{Binding BrowsePatchPackCommand}" Margin="8,0,0,0"/>
                                <Button Content="Load Patch Pack" Command="{Binding LoadPatchPackCommand}" Margin="8,0,0,0"/>
                                <Button Content="Export Patch Pack" Command="{Binding ExportPatchPackCommand}" Margin="8,0,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,8,0,0">
                                <CheckBox Content="Strict (require exact source hash)"
                                          IsChecked="{Binding IsStrictPatchApply}"
                                          VerticalAlignment="Center"
                                          Margin="0,0,12,0"/>
                                <Button Content="Preview Patch Apply" Command="{Binding PreviewPatchPackCommand}"/>
                                <Button Content="Apply Patch Pack" Command="{Binding ApplyPatchPackCommand}" Margin="8,0,0,0"/>
                                <Button Content="Restore Last Backup" Command="{Binding RestoreBackupCommand}" Margin="8,0,0,0"/>
                                <TextBlock Text="{Binding SavePatchApplySummary}" Margin="12,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>

                            <TextBlock Grid.Row="2"
                                       Grid.ColumnSpan="2"
                                       Text="{Binding SavePatchMetadataSummary}"
                                       Margin="0,8,0,0"
                                       TextWrapping="Wrap"/>

                            <DataGrid Grid.Row="3"
                                      ItemsSource="{Binding SavePatchOperations}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      Margin="0,8,8,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="*"/>
                                    <DataGridTextColumn Header="Field Path" Binding="{Binding FieldPath}" Width="2*"/>
                                    <DataGridTextColumn Header="Field Id" Binding="{Binding FieldId}" Width="*"/>
                                    <DataGridTextColumn Header="Type" Binding="{Binding ValueType}" Width="*"/>
                                    <DataGridTextColumn Header="Old" Binding="{Binding OldValue}" Width="*"/>
                                    <DataGridTextColumn Header="New" Binding="{Binding NewValue}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <DataGrid Grid.Row="3"
                                      Grid.Column="1"
                                      ItemsSource="{Binding SavePatchCompatibility}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      Margin="0,8,0,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="*"/>
                                    <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="*"/>
                                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="2*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </GroupBox>

                    <Grid Grid.Row="4" Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock TextWrapping="Wrap"
                                   Text="Full save schema editing is profile-driven. Use field ids or schema paths in Node Path. Diff preview lists changed offsets before save write."/>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Text="{Binding SaveSearchQuery, UpdateSourceTrigger=PropertyChanged}" Width="320"/>
                        </StackPanel>
                        <Grid Grid.Row="2" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <DataGrid ItemsSource="{Binding FilteredSaveFields}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      Margin="0,0,8,0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="2*"/>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Type" Binding="{Binding ValueType}" Width="*"/>
                                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <ListBox Grid.Column="1" ItemsSource="{Binding SaveDiffPreview}"/>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Profiles &amp; Updates">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Check Updates" Command="{Binding CheckUpdatesCommand}"/>
                        <Button Content="Install Selected Profile" Command="{Binding InstallUpdateCommand}" Margin="8,0,0,0"/>
                        <Button Content="Rollback Selected Profile" Command="{Binding RollbackProfileUpdateCommand}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <Grid Grid.Row="1" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="Mod Compatibility Studio" Margin="0,0,8,0">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Base Profile" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Column="1" Text="{Binding OnboardingBaseProfileId}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="1" Text="Draft Profile Id" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding OnboardingDraftProfileId}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="2" Text="Display Name" VerticalAlignment="Center" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding OnboardingDisplayName}" Margin="0,0,0,6"/>

                                <TextBlock Grid.Row="3" Text="Launch Samples" VerticalAlignment="Top" Margin="0,0,8,6"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding OnboardingLaunchSample}" AcceptsReturn="True" Height="90" VerticalScrollBarVisibility="Auto" Margin="0,0,0,6"/>

                                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal">
                                    <Button Content="Scaffold Draft" Command="{Binding ScaffoldModProfileCommand}"/>
                                    <Button Content="Export Calibration Artifact" Command="{Binding ExportCalibrationArtifactCommand}" Margin="8,0,0,0"/>
                                    <Button Content="Build Compatibility Report" Command="{Binding BuildCompatibilityReportCommand}" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <GroupBox Grid.Column="1" Header="Ops Hardening">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Output Dir:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBox Text="{Binding SupportBundleOutputDirectory}" Width="420"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Export Support Bundle" Command="{Binding ExportSupportBundleCommand}"/>
                                    <Button Content="Export Telemetry Snapshot" Command="{Binding ExportTelemetrySnapshotCommand}" Margin="8,0,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Row="2" Text="{Binding ModCompatibilitySummary}" TextWrapping="Wrap" Margin="0,8,0,0"/>

                                <ListBox Grid.Row="3" ItemsSource="{Binding ModCompatibilityRows}" Margin="0,8,0,0"/>
                            </Grid>
                        </GroupBox>
                    </Grid>

                    <Grid Grid.Row="2" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <GroupBox Header="Update Feed" Margin="0,0,8,0">
                            <ListBox ItemsSource="{Binding Updates}" />
                        </GroupBox>
                        <GroupBox Grid.Column="1" Header="Onboarding / Ops Summary">
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding OnboardingSummary}" TextWrapping="Wrap"/>
                                <Separator Margin="0,8,0,8"/>
                                <TextBlock Text="{Binding OpsArtifactSummary}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Unit Roster">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="200" Text="{Binding RosterSearchQuery, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="  Faction:" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Width="120" Text="{Binding RosterSelectedFaction, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Load Roster" Command="{Binding LoadRosterCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" Margin="0,10,0,0"
                              ItemsSource="{Binding RosterEntries}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*"/>
                            <DataGridTextColumn Header="Faction" Binding="{Binding Faction}" Width="120"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="Kind" Binding="{Binding KindLabel}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Spawner">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Header="Spawn Configuration">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Unit ID:" VerticalAlignment="Center" Margin="0,0,8,6"/>
                            <TextBox Grid.Column="1" Text="{Binding EnhancedSpawnUnitId}" Margin="0,0,8,6"/>
                            <TextBlock Grid.Column="2" Text="Faction:" VerticalAlignment="Center" Margin="0,0,8,6"/>
                            <TextBox Grid.Column="3" Text="{Binding EnhancedSpawnFaction}" Margin="0,0,0,6"/>
                            <TextBlock Grid.Row="1" Text="Quantity:" VerticalAlignment="Center" Margin="0,0,8,6"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EnhancedSpawnQuantity}" Margin="0,0,8,6"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="Mode:" VerticalAlignment="Center" Margin="0,0,8,6"/>
                            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding EnhancedSpawnMode}" Margin="0,0,0,6"/>
                            <StackPanel Grid.Row="2" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="0,6,0,0">
                                <Button Content="Spawn" Command="{Binding ExecuteEnhancedSpawnCommand}" Padding="12,4"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </Grid>
            </TabItem>

            <TabItem Header="Faction Dashboard">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Refresh Dashboard" Command="{Binding RefreshDashboardCommand}"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" Margin="0,10,0,0"
                              ItemsSource="{Binding FactionSnapshots}"
                              AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Faction" Binding="{Binding FactionName}" Width="*"/>
                            <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="100"/>
                            <DataGridTextColumn Header="Units" Binding="{Binding UnitCount}" Width="80"/>
                            <DataGridTextColumn Header="Planets" Binding="{Binding PlanetCount}" Width="80"/>
                            <DataGridTextColumn Header="Tech" Binding="{Binding TechLevel}" Width="60"/>
                            <DataGridTextColumn Header="Updated" Binding="{Binding LastUpdated}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Planet Manager">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Load Planets" Command="{Binding LoadPlanetsCommand}"/>
                        <TextBlock Text="  Planet ID:" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Width="160" Text="{Binding SelectedPlanetId, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="New Owner:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="140" Text="{Binding TransferTargetFaction, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Transfer Ownership" Command="{Binding TransferOwnershipCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <DataGrid Grid.Row="2" Margin="0,10,0,0"
                              ItemsSource="{Binding PlanetEntries}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Planet" Binding="{Binding DisplayName}" Width="*"/>
                            <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" Width="120"/>
                            <DataGridTextColumn Header="Station Lv" Binding="{Binding StationLevel}" Width="80"/>
                            <DataGridTextColumn Header="Buildings" Binding="{Binding Buildings}" Width="180"/>
                            <DataGridTextColumn Header="Corruption" Binding="{Binding Corruption}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Fleet Manager">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Load Fleets" Command="{Binding LoadFleetsCommand}"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" Margin="0,10,0,0"
                              ItemsSource="{Binding FleetEntries}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fleet" Binding="{Binding FleetId}" Width="*"/>
                            <DataGridTextColumn Header="Faction" Binding="{Binding Faction}" Width="120"/>
                            <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="140"/>
                            <DataGridTextColumn Header="Units" Binding="{Binding UnitCount}" Width="60"/>
                            <DataGridTextColumn Header="Composition" Binding="{Binding Composition}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Ownership">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Header="Faction Switch">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <TextBlock Text="Target Faction:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Width="160" Text="{Binding SwitchFactionTarget, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Content="Switch Faction" Command="{Binding SwitchFactionCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Grid.Row="1" Header="Ownership Transfer" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <TextBlock Text="Transfer To:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Width="160" Text="{Binding TransferTargetFaction, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Content="Transfer" Command="{Binding TransferOwnershipCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </GroupBox>
                    <TextBlock Grid.Row="2" Text="Select a unit in-game before transferring ownership. Faction switch changes the player-controlled faction during galactic conquest."
                               TextWrapping="Wrap" Margin="0,12,0,0" Opacity="0.6"/>
                </Grid>
            </TabItem>

            <TabItem Header="AI Control">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Header="AI Suspension">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <TextBlock Text="Suspend (sec):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Width="80" Text="{Binding AiSuspendSeconds, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Content="Suspend AI" Command="{Binding ExecuteAiControlCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Grid.Row="1" Header="Cooldown Reset" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <TextBlock Text="Unit ID (blank=all):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Width="160" Text="{Binding CooldownTargetUnitId, UpdateSourceTrigger=PropertyChanged}"/>
                            <Button Content="Reset Cooldowns" Command="{Binding ResetCooldownsCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </GroupBox>
                    <TextBlock Grid.Row="2" Text="AI suspension freezes faction AI decision-making for the specified duration. Cooldown reset clears ability timers on the target unit or all player units."
                               TextWrapping="Wrap" Margin="0,12,0,0" Opacity="0.6"/>
                </Grid>
            </TabItem>

            <TabItem Header="Camera Director">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Header="Camera Commands">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <TextBlock Text="Command:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Width="300" Text="{Binding CameraCommand, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="capture, play, stop, letterbox_on, letterbox_off, freeze, unfreeze"/>
                            <Button Content="Execute" Command="{Binding ExecuteCameraCommand}" Margin="8,0,0,0"/>
                        </StackPanel>
                    </GroupBox>
                    <TextBlock Grid.Row="1" Text="Camera director commands: capture (keyframe), play, stop, letterbox_on, letterbox_off, freeze, unfreeze. Used for cinematic content creation."
                               TextWrapping="Wrap" Margin="0,12,0,0" Opacity="0.6"/>
                </Grid>
            </TabItem>

            <TabItem Header="Story Events">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Load Events" Command="{Binding LoadStoryEventsCommand}"/>
                        <TextBlock Text="  Custom Event:" VerticalAlignment="Center" Margin="12,0,8,0"/>
                        <TextBox Width="200" Text="{Binding CustomStoryEvent, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Fire Event" Command="{Binding FireStoryEventCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Selected:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="300"
                                  ItemsSource="{Binding StoryEventEntries}"
                                  SelectedValue="{Binding SelectedStoryEvent}"
                                  SelectedValuePath="EventId"
                                  DisplayMemberPath="DisplayName"/>
                    </StackPanel>
                    <DataGrid Grid.Row="2" Margin="0,10,0,0"
                              ItemsSource="{Binding StoryEventEntries}"
                              AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Event ID" Binding="{Binding EventId}" Width="*"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*"/>
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Damage Log">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Refresh" Command="{Binding RefreshDamageLogCommand}"/>
                        <Button Content="Detect Mod Conflicts" Command="{Binding DetectModConflictsCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" Margin="0,10,0,0"
                              ItemsSource="{Binding DamageLogEntries}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="100"/>
                            <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="*"/>
                            <DataGridTextColumn Header="Target" Binding="{Binding Target}" Width="*"/>
                            <DataGridTextColumn Header="Damage" Binding="{Binding Damage}" Width="80"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Diplomacy">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Load Diplomacy" Command="{Binding LoadDiplomacyCommand}"/>
                        <Button Content="Set Relation" Command="{Binding SetDiplomacyRelationCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="Diplomacy management is mod-aware. Relations between factions can be read and modified when the active profile supports diplomacy hooks. Use Set Relation to change the default hostile/allied/neutral state between faction pairs."
                               TextWrapping="Wrap" Margin="0,12,0,0" Opacity="0.6"/>
                </Grid>
            </TabItem>

            <TabItem Header="Corruption">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <GroupBox Header="Planet Corruption">
                        <Grid Margin="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Planet ID:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Grid.Column="1" Text="{Binding CorruptionPlanetId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="2" Text="Type:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Grid.Column="3" Text="{Binding CorruptionType, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Racketeering, Bribery, Piracy, Kidnapping, Robbery"/>
                        </Grid>
                    </GroupBox>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Apply Corruption" Command="{Binding SetCorruptionCommand}"/>
                        <Button Content="Remove Corruption" Command="{Binding RemoveCorruptionCommand}" Margin="8,0,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Row="2" Text="Corruption is a Forces of Corruption mechanic. Apply or remove corruption types on specific planets. Valid types: Racketeering, Bribery, Piracy, Kidnapping, Robbery."
                               TextWrapping="Wrap" Margin="0,12,0,0" Opacity="0.6"/>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Background="#0D1422" BorderBrush="#2E3E5C" BorderThickness="1" Padding="10" Margin="0,12,0,0">
            <TextBlock Text="{Binding Status}"/>
        </Border>
    </Grid>
</Window>
