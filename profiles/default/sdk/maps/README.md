# Capability Maps

Capability maps are keyed by binary `fingerprintId` and define per-operation anchor requirements.

Filename convention:

- `<fingerprintId>.json`

These files are generated/reviewed under the R&D track and promoted only after deterministic validation.
